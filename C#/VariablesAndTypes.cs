using System;

namespace Form
{
    class Program
    {
        static void Main(string[] args)
        {
            // Create Variables
            // String Variable called name with a value of Shadow
            string name = "Shadow";
            // String Variable called breed with a value of Golden Retriever
            string breed = "Golden Retriever";
            // Int variable named age with a value of 5
            int age = 5;
            // Double Variable named weight with a value of 65.22
            double weight = 65.22;
            // Bool variable named spayed with a value of True
            bool spayed = true;
            // Decimal is a variable that is used for finacial applications
            decimal itemCost = 65.99m;

            // Print variables to the console
            Console.WriteLine(name);
            Console.WriteLine(breed);
            Console.WriteLine(age);
            Console.WriteLine(weight);
            Console.WriteLine(spayed);
            Console.WriteLine(itemCost);
        }
    }
}
